# Virtual environment
venv/
.venv/

# Local database (runtime state — do not commit; it also holds the admin password hash)
charging.db
*.db

# Streamlit secrets (database connection string, etc.) — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
